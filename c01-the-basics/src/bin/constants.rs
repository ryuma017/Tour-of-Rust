#![allow(clippy::approx_constant)]

const PI: f32 = 3.14159; //定数は明示的な型指定が必須

fn main() {
    println!(
        "To make an apple {} from scratch, you must first create a universe.",
        PI
    );
}